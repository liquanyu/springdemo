server:
  port: 9091
spring:
  application:
    name: service-feign
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://root:123456@localhost:10001/eureka/
